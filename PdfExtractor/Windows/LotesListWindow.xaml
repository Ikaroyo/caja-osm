<Window x:Class="PdfExtractor.Windows.LotesListWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Lista de Lotes" Height="600" Width="1000" WindowStartupLocation="CenterOwner">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <TextBlock Grid.Row="0" Text="Lista de Lotes Procesados" FontWeight="Bold" FontSize="14" Margin="0,0,0,15"/>
        
        <!-- Filtros Principales -->
        <GroupBox Grid.Row="1" Header="🔍 Filtros" Margin="0,0,0,10">
            <Grid Margin="10">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                
                <!-- Primera fila de filtros -->
                <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,10">
                    <TextBlock Text="Día:" VerticalAlignment="Center" Margin="0,0,5,0" Width="60"/>
                    <ComboBox x:Name="cmbDiaFilter" Width="120" Height="25" SelectionChanged="Filter_Changed" Margin="0,0,15,0"/>
                    
                    <TextBlock Text="Caja:" VerticalAlignment="Center" Margin="0,0,5,0" Width="60"/>
                    <ComboBox x:Name="cmbCajaFilter" Width="100" Height="25" SelectionChanged="Filter_Changed" Margin="0,0,15,0"/>
                    
                    <TextBlock Text="Usuario:" VerticalAlignment="Center" Margin="0,0,5,0" Width="60"/>
                    <ComboBox x:Name="cmbUsuarioFilter" Width="120" Height="25" SelectionChanged="Filter_Changed" Margin="0,0,15,0"/>
                    
                    <TextBlock Text="Estado:" VerticalAlignment="Center" Margin="0,0,5,0" Width="60"/>
                    <ComboBox x:Name="cmbEstadoFilter" Width="120" Height="25" SelectionChanged="Filter_Changed"/>
                </StackPanel>
                
                <!-- Segunda fila de filtros -->
                <StackPanel Grid.Row="1" Orientation="Horizontal">
                    <TextBlock Text="Desde:" VerticalAlignment="Center" Margin="0,0,5,0" Width="60"/>
                    <DatePicker x:Name="dpFechaDesde" Width="120" Height="25" SelectedDateChanged="Filter_Changed" Margin="0,0,15,0"/>
                    
                    <TextBlock Text="Hasta:" VerticalAlignment="Center" Margin="0,0,5,0" Width="60"/>
                    <DatePicker x:Name="dpFechaHasta" Width="120" Height="25" SelectedDateChanged="Filter_Changed" Margin="0,0,25,0"/>
                    
                    <Button x:Name="btnClearFilter" Content="🗑️ Limpiar Filtros" Width="120" Height="25" Click="BtnClearFilter_Click" Margin="0,0,10,0" Background="LightCoral"/>
                    <Button x:Name="btnMarcarDepositado" Content="✅ Marcar Depositado" Width="140" Height="25" Click="BtnMarcarDepositado_Click" Background="LightGreen" Margin="0,0,10,0"/>
                    <Button x:Name="btnBorrarLotes" Content="🗑️ Borrar Seleccionados" Width="140" Height="25" Click="BtnBorrarLotes_Click" Background="LightPink"/>
                </StackPanel>
            </Grid>
        </GroupBox>
        
        <DataGrid Grid.Row="3" x:Name="dgLotes" AutoGenerateColumns="False" CanUserAddRows="False" 
                  IsReadOnly="True" GridLinesVisibility="All" HeadersVisibility="All" SelectionMode="Extended"
                  MouseDoubleClick="DgLotes_MouseDoubleClick">
            <DataGrid.Columns>
                <DataGridCheckBoxColumn Header="" Binding="{Binding IsSelected}" Width="30"/>
                <DataGridTextColumn Header="Estado" Binding="{Binding EstadoDepositado}" Width="80"/>
                <DataGridTextColumn Header="Día" Binding="{Binding Dia}" Width="80"/>
                <DataGridTextColumn Header="Fecha" Binding="{Binding FechaString}" Width="90"/>
                <DataGridTextColumn Header="Lote" Binding="{Binding Lote}" Width="100"/>
                <DataGridTextColumn Header="Usuario" Binding="{Binding Usuario}" Width="100"/>
                <DataGridTextColumn Header="Caja" Binding="{Binding Caja}" Width="80"/>
                <DataGridTextColumn Header="OSM" Binding="{Binding OSMFormatted}" Width="120"/>
                <DataGridTextColumn Header="MUNI" Binding="{Binding MUNIFormatted}" Width="120"/>
                <DataGridTextColumn Header="Crédito" Binding="{Binding CreditoFormatted}" Width="120"/>
                <DataGridTextColumn Header="Débito" Binding="{Binding DebitoFormatted}" Width="120"/>
                <DataGridTextColumn Header="Créd+Déb" Binding="{Binding CreditoDebitoFormatted}" Width="120"/>
                <DataGridTextColumn Header="Cheque" Binding="{Binding ChequeFormatted}" Width="120"/>
                <DataGridTextColumn Header="Efectivo" Binding="{Binding EfectivoFormatted}" Width="120"/>
                <DataGridTextColumn Header="Observaciones" Binding="{Binding Observaciones}" Width="200"/>
            </DataGrid.Columns>
        </DataGrid>
        
        <Grid Grid.Row="4" Margin="0,10,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            
            <StackPanel Grid.Column="0" Orientation="Horizontal">
                <TextBlock Text="Subtotales filtrados:" FontWeight="Bold" VerticalAlignment="Center" Margin="0,0,15,0"/>
                <TextBlock Text="OSM:" VerticalAlignment="Center" Margin="0,0,5,0"/>
                <TextBlock x:Name="txtTotalOSM" Text="$ 0,00" VerticalAlignment="Center" FontWeight="Bold" Margin="0,0,15,0"/>
                <TextBlock Text="MUNI:" VerticalAlignment="Center" Margin="0,0,5,0"/>
                <TextBlock x:Name="txtTotalMUNI" Text="$ 0,00" VerticalAlignment="Center" FontWeight="Bold" Margin="0,0,15,0"/>
                <TextBlock Text="Efectivo:" VerticalAlignment="Center" Margin="0,0,5,0"/>
                <TextBlock x:Name="txtTotalEfectivo" Text="$ 0,00" VerticalAlignment="Center" FontWeight="Bold" Margin="0,0,15,0"/>
                <TextBlock Text="Créd+Déb:" VerticalAlignment="Center" Margin="0,0,5,0"/>
                <TextBlock x:Name="txtTotalCreditoDebito" Text="$ 0,00" VerticalAlignment="Center" FontWeight="Bold"/>
            </StackPanel>
            
            <Button Grid.Column="1" x:Name="btnClose" Content="Cerrar" Width="80" Height="30" Click="BtnClose_Click"/>
        </Grid>
    </Grid>
</Window>
